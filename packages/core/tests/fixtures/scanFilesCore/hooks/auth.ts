import type { Hook } from "../../../types/hook";

const hook: Hook = {
    enable: true,
    name: "auth",
    deps: [],
    handler: async () => {
        // fixture hook
    }
};

export default hook;
